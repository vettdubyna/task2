---
kind: pipeline
type: kubernetes
name: default
metadata:
  namespace: drone
steps:
- name: build
  image: docker:18.09
  environment:
  DOCKER_USERNAME:
    from_secret: username
  commands:
    - echo $DOCKER_USERNAME
    - docker build -t quay.io/vdubyna/test-img:latest .

